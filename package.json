{
  "name": "fylo",
  "version": "1.0.0",
  "description": "Fylo landing page",
  "main": "index.js",
  "scripts": {
    "watch:sass": "node-sass -w style/sass/main.scss style/css/style.css",
    "devserver": "live-server",
    "start": "npm-run-all --parallel watch:sass devserver",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ahmad",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^9.0.0",
    "npm-run-all": "^4.1.5"
  }
}